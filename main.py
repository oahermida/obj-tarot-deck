# main.py — menu loop and program entry point only. (Phase 8)
#
# Imports everything, defines nothing. The numbered-menu pattern from the three
# earlier projects: new reading (pick a spread, shuffle, draw, display), later
# export and history, quit — plus the input validation those projects already
# taught, so bad input never crashes it.
#
# No class definitions here. If a class starts appearing in this file, it
# belongs in card.py, deck.py, or spread.py instead.
#
# Expect this file to be short. When the objects carry the behaviour, the entry
# point shrinks to traffic direction — comparing its final length against the
# Habit Tracker's main.py is part of the point of the phase.
